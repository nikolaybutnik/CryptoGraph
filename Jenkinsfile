pipeline{
    agent any
    stages{
        stage("Build"){
            steps{
                echo "========Installing dependencies========"
                sh 'npm install'
            }
        }
    }
}